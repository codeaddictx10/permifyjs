# Changelog

## 1.0.0-beta.2

### Minor Changes

- eb0e466: Add first-class Fastify and TypeORM adapters.

  `@permifyjs/fastify` now ships a maintained Fastify integration for permission and role guards.

  `@permifyjs/typeorm` now ships as a supported adapter with CLI init and migration support, aligning the published package set with the repo's documented capabilities.

## 1.0.0-beta.1

- Initial TypeORM adapter release
